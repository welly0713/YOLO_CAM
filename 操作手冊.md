# YOLO_CAM 操作手冊

## 1. 用途

本程式可以使用：

- Windows 電腦內建或 USB Webcam 做測試
- RTSP 監視器做正式監測

預設每秒偵測一次。最近 15 秒的 15 次取樣中，至少 10 次偵測到人時，會在 CMD 顯示告警。啟用 Email 後，告警信會附上該偵測窗內所有偵測到人的完整畫面。

## 2. 使用前準備

### 本機 Python 模式

建議使用 Python 3.11 或更新版本。先開啟 PowerShell，進入專案資料夾：

```powershell
cd C:\Users\Welly\Downloads\tmp\Code\YOLO_CAM
```

建立虛擬環境並安裝套件：

```powershell
py -3.11 -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -r requirements.txt
```

若 PowerShell 阻擋啟用虛擬環境，可改用：

```powershell
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

### Docker 模式

需要先安裝並啟動 Docker Desktop。確認指令可用：

```powershell
docker --version
docker compose version
```

如果出現「docker 無法辨識」，代表 Docker Desktop 尚未安裝或尚未加入 PATH。安裝後請關閉並重新開啟 PowerShell。

## 3. 第一次使用本機鏡頭測試

目前專案內的 .env 已設定為本機鏡頭模式：

```dotenv
CAMERA_SOURCE=webcam
WEBCAM_INDEX=0
SHOW_PREVIEW=true
```

確認 Windows 已允許桌面應用程式使用相機後執行：

```powershell
python -m app.main
```

第一次執行可能會下載 YOLO26m 到 `/app/yolo26m.pt`，請等待下載完成。程式啟動後會出現 YOLO_CAM preview 視窗，視窗上會顯示：

- people=0：目前畫面沒有偵測到人
- people=1 或更高：目前畫面偵測到人
- samples=x/15：最近 15 次取樣中有幾次偵測到人

停止方式：

- 在預覽視窗按 q
- 在預覽視窗按 Esc
- 回到 PowerShell 按 Ctrl+C

## 4. 切換不同本機鏡頭

如果 index 0 不是想測試的鏡頭，停止程式後編輯 .env：

```dotenv
WEBCAM_INDEX=1
```

依序嘗試 0、1、2。每次只應有一個程式占用該鏡頭，Teams、Zoom、瀏覽器相機頁面可能會造成讀取失敗。

若預覽太慢，可降低解析度：

```dotenv
WEBCAM_WIDTH=640
WEBCAM_HEIGHT=480
```

## 5. 測試告警規則

正常啟動後，對著鏡頭停留至少約 15 秒。預設規則不是「連續 15 秒每秒都要有人」，而是：

```text
最近 15 次取樣中，至少 10 次有人
```

測試時可以暫時縮短視窗：

```dotenv
DETECTION_WINDOW_SECONDS=6
SAMPLE_INTERVAL_SECONDS=1
DETECTION_MIN_RATIO=0.6666667
ALERT_COOLDOWN_SECONDS=0
```

這樣最近 6 次取樣中至少 4 次偵測到人就會告警。測試完建議恢復成 15 秒與 300 秒 cooldown。

## 6. 切換到 RTSP 監視器

停止程式並編輯 .env：

```dotenv
CAMERA_SOURCE=rtsp
SHOW_PREVIEW=false
RTSP_HOST=監視器IP
RTSP_PORT=554
RTSP_USERNAME=監視器帳號
RTSP_PASSWORD=監視器密碼
RTSP_PATH=/Streaming/Channels/101
```

不同品牌的 RTSP path 不同，常見例子：

```text
/Streaming/Channels/101
/h264Preview_01_main
/live/ch00_0
```

請以監視器手冊或廠商設定為準。若有完整 RTSP URL，也可以只填：

```dotenv
RTSP_URL=rtsp://帳號:密碼@IP:554/實際路徑
```

RTSP_URL 有值時會優先於 RTSP_HOST 等分開欄位。

在 Windows 本機執行：

```powershell
python -m app.main
```

程式不會把含密碼的 RTSP URL 印到 log。

## 7. 使用 Docker 啟動 RTSP 監測

Docker 主要用於 RTSP 監測，不建議拿來讀取 Windows 本機 Webcam。

`yolo-cam` 容器已限制最多使用 2 GB RAM 與 2 顆 CPU。若超過 2 GB，Docker 會終止該容器，並依自動重啟設定重新啟動；這是避免 Docker Desktop/WSL 佔滿主機記憶體的保護措施。

啟動前，請確認 .env 至少包含：

```dotenv
CAMERA_SOURCE=rtsp
SHOW_PREVIEW=false
RTSP_HOST=實際IP
RTSP_PORT=554
RTSP_USERNAME=實際帳號
RTSP_PASSWORD=實際密碼
RTSP_PATH=實際路徑
```

然後執行：

```powershell
cd C:\Users\Welly\Downloads\tmp\Code\YOLO_CAM
docker compose up --build
```

背景執行：

```powershell
docker compose up --build -d
docker compose logs -f yolo-cam
```

停止：

```powershell
docker compose down
```

第一次啟動可能下載 YOLO 模型。模型快取會保存於 Docker named volume，之後重啟不必重複下載。

## 8. 啟用 Email 告警

目前只要設定 EMAIL_ENABLED=false，告警就只會顯示在 CMD。確認 SMTP 資料正確後，再修改：

```dotenv
EMAIL_ENABLED=true
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=寄件帳號
SMTP_PASSWORD=SMTP密碼或應用程式密碼
SMTP_USE_TLS=true
EMAIL_FROM=寄件地址
EMAIL_TO=收件地址
EMAIL_SUBJECT=YOLO CAM person detected
```

程式會同時保留 CMD 輸出與 Email。每封告警信都會附上目前偵測窗內的所有有人畫面；同張畫面中有多位人時會完整保留，不會只附第一位人員。Email 設定中的「SFTP」應為「SMTP」。

建議先用短視窗驗證附件是否正常：

```dotenv
DETECTION_WINDOW_SECONDS=6
SAMPLE_INTERVAL_SECONDS=1
DETECTION_MIN_RATIO=0.6666667
ALERT_COOLDOWN_SECONDS=0
```

在鏡頭前停留約 6 秒，收到信後確認附件數量與畫面內容。測試完成後請將視窗與 cooldown 恢復正式值。請注意：同一事件只會寄一次信；必須等人離開到視窗不再符合條件，才會視為下一個事件。

## 9. 使用 Web 中控台

將系統部署到機房 VM 後，在中控室電腦使用瀏覽器開啟：

```text
http://VM_IP:8090
```

首頁會顯示三個即時監視器視窗；上方狀態列會顯示已連線的攝影機數量。第一次開啟時，按右上角「啟用告警聲」，瀏覽器才可播放告警音。

設定方式：

1. 點選右上角「設定攝影機與 ROI」。
2. 為最多四台 IP Camera 填入 RTSP URL，勾選啟用。
3. 在每台預覽畫面上依序點選取水口範圍的三個以上角點。
4. 設定人員在 ROI 內的逗留秒數，建議先使用 10 秒。
5. 按「儲存並套用設定」。套用時攝影機會短暫重新連線。

畫面會持續串流，但 YOLO 對每台已啟用攝影機只在每秒執行一次。相同人員在 ROI 內持續逗留達設定時間後，畫面會變紅並播放三聲告警音。

## 10. 常見問題

| 問題 | 處理方式 |
|---|---|
| docker 無法辨識 | 安裝並啟動 Docker Desktop，重新開啟 PowerShell |
| Webcam 無法開啟 | 嘗試 WEBCAM_INDEX=1；關閉 Teams/Zoom/瀏覽器相機使用；檢查 Windows 相機權限 |
| 沒有預覽視窗 | 確認安裝的是 opencv-python，且 SHOW_PREVIEW=true |
| 預覽視窗打不開但程式繼續跑 | 將 SHOW_PREVIEW=false；Docker/headless 環境不要開預覽 |
| 尚未取得 RTSP 影像 | 檢查 IP、port、帳密、RTSP path、防火牆與攝影機網路連通性 |
| 一直顯示無人 | 調低 PERSON_CONFIDENCE，例如 0.4；確認光線、角度與模型畫面品質 |
| 一直重複告警 | 增加 ALERT_COOLDOWN_SECONDS，或確認人離開後視窗有恢復為不符合條件 |
| YOLO 模型下載失敗 | 確認主機/容器可連網；也可以預先下載模型並設定 MODEL_PATH 為檔案路徑 |
| Email 寄不出去 | 檢查 SMTP host/port/TLS、收件人與應用程式密碼；先保持 EMAIL_ENABLED=false 驗證偵測本身 |
| 收到 Email 但沒有附件 | 確認告警觸發前確實有偵測到人；查看 log 是否有 JPEG 編碼錯誤；確認收件服務沒有移除附件 |
| 附件張數比預期少 | 附件只包含目前滑動視窗內偵測到人的取樣，不包含無人、過期或 YOLO 推論失敗的畫面 |

## 11. 日常操作建議

1. 每次修改 .env 後重新啟動程式或重建容器。
2. 正式監測使用 CAMERA_SOURCE=rtsp、SHOW_PREVIEW=false。
3. 每次更新模型或套件後，先使用 Webcam 做短時間測試。
4. 確認 CMD 有穩定輸出取樣資訊，再啟用 Email。
5. 不要把 .env、RTSP URL、SMTP 密碼貼到聊天群組或提交到版本控制。
