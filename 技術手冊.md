# YOLO_CAM 技術手冊

## 1. 文件資訊

| 項目 | 內容 |
|---|---|
| 專案 | YOLO_CAM |
| 目的 | 從本機鏡頭或 RTSP 監視器偵測畫面中的人員 |
| 主要語言 | Python 3.11+ |
| 推論框架 | Ultralytics YOLO |
| 影像處理 | OpenCV |
| 設定方式 | .env 環境變數 |
| 部署方式 | Windows 本機執行或 Docker Compose |
| 目前告警 | CMD 輸出；可選 SMTP 郵件，並附上偵測到人員的畫面 |

## 2. 系統架構

```text
本機 Webcam / RTSP Camera
          │
          ▼
   LatestFrameReader
   背景執行緒持續讀取最新畫面
          │
          ▼
       YOLO 推論
   只保留 COCO person class=0
          │
          ▼
    DetectionWindow
  滑動視窗統計有人/無人
          │
          ▼
      AlertGate
  避免同一事件重複告警
          │
          ├── CMD 輸出
          └── SMTP Email（可選）
              └── 將目前視窗內所有有人畫面轉為 JPEG 附件
```

程式不會將影像或錄影寫入磁碟。告警前會在記憶體保留目前滑動視窗內的取樣；告警時，僅把其中偵測到人的完整畫面轉成 JPEG 郵件附件後送出。

## 3. 專案結構

```text
YOLO_CAM/
├── app/
│   ├── __init__.py       Python package 定義
│   ├── config.py         讀取與驗證 .env
│   ├── camera.py         Webcam/RTSP 背景讀取與斷線重連
│   ├── detection.py      滑動視窗與告警閘門
│   ├── notifier.py       CMD 與 SMTP 通知
│   └── main.py           程式入口與主迴圈
├── tests/
│   ├── test_detection.py 純邏輯測試
│   └── test_notifier.py  郵件 JPEG 附件測試
├── .env                 本機設定，不應提交到 Git
├── .env.example         設定範例
├── requirements.txt      Python 套件
├── Dockerfile            Docker 映像建置規則
├── docker-compose.yml    Docker 服務與模型快取 volume
├── README.md             專案簡介
├── 技術手冊.md
└── 操作手冊.md
```

## 4. 影像來源

### 4.1 RTSP 模式

設定：

```dotenv
CAMERA_SOURCE=rtsp
RTSP_HOST=192.168.1.100
RTSP_PORT=554
RTSP_USERNAME=admin
RTSP_PASSWORD=change-me
RTSP_PATH=/Streaming/Channels/101
```

程式會組合成：

```text
rtsp://帳號:密碼@IP:Port/路徑
```

也可以使用 RTSP_URL 直接指定完整 URL；有值時會優先使用 RTSP_URL。帳號與密碼會進行 URL encoding，但仍不可把 .env 分享出去。

RTSP_TRANSPORT=tcp 會透過 OpenCV FFmpeg 設定 RTSP transport。TCP 通常比 UDP 穩定，但延遲可能稍高。

### 4.2 Webcam 模式

設定：

```dotenv
CAMERA_SOURCE=webcam
WEBCAM_INDEX=0
WEBCAM_WIDTH=1280
WEBCAM_HEIGHT=720
WEBCAM_FPS=0
SHOW_PREVIEW=true
```

Windows 會優先使用 DirectShow；無法開啟時會回退到 OpenCV 自動選擇的 backend。WEBCAM_INDEX=0 通常是內建鏡頭，其他鏡頭可嘗試 1、2。

預覽視窗只適合在有桌面的本機環境使用。Docker 或無 GUI 環境應設定 SHOW_PREVIEW=false。

## 5. 偵測與告警演算法

### 5.1 YOLO 推論

- 預設模型：yolo26n.pt
- 預設裝置：cpu
- 預設信心度：PERSON_CONFIDENCE=0.5
- 推論類別：COCO person，class id 為 0
- 一個畫面只要偵測到至少一個人，就記錄為 positive sample

### 5.2 滑動視窗

預設設定為：

```text
SAMPLE_INTERVAL_SECONDS = 1 秒
DETECTION_WINDOW_SECONDS = 15 秒
DETECTION_MIN_RATIO = 2/3
```

視窗樣本數為：

```text
ceil(DETECTION_WINDOW_SECONDS / SAMPLE_INTERVAL_SECONDS)
= ceil(15 / 1)
= 15 次
```

所需最少有人樣本為：

```text
ceil(15 × 0.6666667) = 10 次
```

因此，最近 15 次取樣中至少 10 次偵測到人，才會進入告警條件。視窗尚未累積滿 15 次時，不會告警。

### 5.3 重複告警控制

AlertGate 會將告警視為一個事件：

1. 視窗第一次符合條件時告警一次。
2. 視窗持續符合條件時不重複告警。
3. 視窗不再符合條件後，事件解除。
4. 新事件仍須符合 ALERT_COOLDOWN_SECONDS 的間隔限制。

預設 cooldown 為 300 秒。若要測試時快速重複告警，可暫時設為 0。

### 5.4 郵件影像附件

當視窗第一次符合告警條件時，`main.py` 會收集該視窗內全部 positive sample 的完整原始畫面，並傳給 `Notifier`。不是只擷取第一位人員或裁切人像，因此同一畫面中出現的多位人員都會保留。

`notifier.py` 會將每張畫面 JPEG 編碼，附件檔名格式為：

```text
person-YYYYMMDD-HHMMSS-01.jpg
person-YYYYMMDD-HHMMSS-02.jpg
```

例如預設 15 秒視窗在第 10 個 positive sample 達到門檻時，信件最多會附上當下視窗裡 15 張畫面；實際數量等於視窗中的有人取樣數。事件持續符合條件時不會重複寄信，因此不會因同一人在鏡頭前停留而持續寄出相同附件。

## 6. 主要模組

### app/config.py

使用 python-dotenv 載入 .env，將文字設定轉為有型別的 Settings。會檢查數字、布林值、來源模式、信心度與視窗參數。

### app/camera.py

LatestFrameReader 以 daemon thread 持續抓取影像，只保留最新 frame。推論速度較慢時不會累積大量舊影像。影像來源斷線或讀取失敗時會依 RTSP_RECONNECT_DELAY_SECONDS 重試。

### app/detection.py

不依賴 OpenCV 或 YOLO，負責：

- 保存最近 N 次布林結果
- 計算 positive sample 數與比例
- 判斷視窗是否達到門檻
- 控制同一事件是否已告警

### app/notifier.py

每次告警一定先印出 CMD 訊息。EMAIL_ENABLED=true 時，會額外透過 SMTP 寄信，並附上目前視窗內每張偵測到人的完整畫面。SMTP 設定不完整、JPEG 編碼失敗或寄信暫時失敗時，主監測程式會記錄錯誤並繼續執行。

### app/main.py

負責載入模型、建立影像讀取器、每秒取樣、執行 YOLO、更新視窗、顯示預覽與觸發通知。

## 7. 設定參數

### 影像來源

| 變數 | 預設值 | 說明 |
|---|---:|---|
| CAMERA_SOURCE | rtsp | rtsp 或 webcam |
| WEBCAM_INDEX | 0 | 本機鏡頭索引 |
| WEBCAM_WIDTH | 1280 | Webcam 要求寬度 |
| WEBCAM_HEIGHT | 720 | Webcam 要求高度 |
| WEBCAM_FPS | 0 | 0 使用鏡頭預設值 |
| SHOW_PREVIEW | webcam 時預設 true | 是否顯示 OpenCV 預覽視窗 |

### RTSP

| 變數 | 預設值 | 說明 |
|---|---:|---|
| RTSP_URL | 空白 | 完整 RTSP URL，優先於分開欄位 |
| RTSP_SCHEME | rtsp | 協定 scheme |
| RTSP_HOST | 空白 | IP 或 DNS |
| RTSP_PORT | 554 | RTSP port |
| RTSP_USERNAME | 空白 | RTSP 帳號 |
| RTSP_PASSWORD | 空白 | RTSP 密碼 |
| RTSP_PATH | /Streaming/Channels/101 | 串流路徑 |
| RTSP_TRANSPORT | tcp | FFmpeg transport |
| RTSP_OPEN_TIMEOUT_SECONDS | 10 | 開啟/讀取 timeout |
| RTSP_RECONNECT_DELAY_SECONDS | 5 | 斷線後重試間隔 |
| MAX_FRAME_AGE_SECONDS | 5 | 影像過期後視為無人 |

### YOLO 與告警

| 變數 | 預設值 | 說明 |
|---|---:|---|
| MODEL_PATH | yolo26n.pt | 模型名稱或檔案路徑 |
| YOLO_DEVICE | cpu | 可改成支援的 GPU device |
| PERSON_CONFIDENCE | 0.5 | 0 到 1 的信心度門檻 |
| SAMPLE_INTERVAL_SECONDS | 1 | 取樣間隔 |
| DETECTION_WINDOW_SECONDS | 15 | 滑動視窗長度 |
| DETECTION_MIN_RATIO | 0.6666667 | 視窗內最少有人比例 |
| ALERT_COOLDOWN_SECONDS | 300 | 新事件最短告警間隔 |

### SMTP

| 變數 | 預設值 | 說明 |
|---|---:|---|
| EMAIL_ENABLED | false | 是否額外寄信；寄出時含有人員畫面 JPEG 附件 |
| SMTP_HOST | 空白 | SMTP server |
| SMTP_PORT | 587 | SMTP port |
| SMTP_USERNAME | 空白 | SMTP 帳號 |
| SMTP_PASSWORD | 空白 | SMTP 密碼 |
| SMTP_USE_TLS | true | 是否使用 STARTTLS |
| EMAIL_FROM | 空白 | 寄件人 |
| EMAIL_TO | 空白 | 收件人，可用逗號分隔多個地址 |
| EMAIL_SUBJECT | YOLO CAM person detected | 郵件主旨 |

## 8. Docker 技術說明

Dockerfile 使用 Python 3.11 slim image，安裝 OpenCV、Ultralytics 與必要的 Linux runtime libraries。docker-compose.yml：

- 以 .env 作為 env_file
- 啟動後執行 python -m app.main
- 使用 restart: unless-stopped
- 將 yolo-cam 限制為最多 2 GB RAM、保留 768 MB、最多使用 2 顆 CPU；若超過硬性 RAM 上限，容器會被終止並由 restart policy 重啟
- 以 ultralytics-cache named volume 保存 YOLO 模型快取

建置與啟動：

```powershell
docker compose up --build
```

背景執行與查看 log：

```powershell
docker compose up --build -d
docker compose logs -f yolo-cam
```

Docker 執行時建議使用 RTSP。Windows 本機 Webcam 不會自動映射到 Docker Desktop Linux container，因此本機鏡頭測試應直接在 Windows Python 環境執行。

## 9. 錯誤處理與可觀測性

- 設定格式錯誤：程式啟動時直接拋出明確錯誤。
- RTSP/Webcam 無法開啟：記錄 warning，等待後自動重試。
- 單張影像 YOLO 推論失敗：該次視為無人，程式繼續執行。
- 影像超過 MAX_FRAME_AGE_SECONDS：該次視為無人。
- SMTP 寄信或影像附件編碼失敗：記錄 exception，監測器不停止。
- Ctrl+C、q、Esc：正常停止影像讀取執行緒。

LOG_LEVEL 支援 DEBUG、INFO、WARNING、ERROR。

## 10. 測試與驗證

安裝開發依賴後可執行：

```powershell
pytest -q
```

測試會驗證時間視窗、告警閘門與 SMTP 信件是否包含每張有人畫面的 JPEG 附件，不需要攝影機、YOLO 模型或實際 SMTP 伺服器。完整系統驗證則需要實際 Webcam 或 RTSP URL。

## 11. 安全與維護注意事項

- .env 含有 RTSP/SMTP 密碼，禁止提交到 Git 或貼到公開 issue。
- 不要把包含帳號密碼的完整 RTSP URL 寫入 log。
- 建議建立專用的監視器只讀帳號與 SMTP 應用程式密碼。
- 若使用 Gmail、Microsoft 365 等服務，通常需要 App Password 或 OAuth/租戶允許的 SMTP 設定，不能直接假設一般登入密碼可用。
- YOLO 模型首次啟動可能需要網路下載；Docker 會用 named volume 保存快取。
- 影像不會保存到磁碟，但會經由 SMTP 傳送給收件人。收件人信箱、轉寄規則與附件保存期限都應依個資與監視規範管理。
- 若日後需要在本機保留截圖，應另外設計檔案保留期限、加密與存取權限，不能假設 Email 附件可作為長期保存機制。
